<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp"

    tools:context=".notasApp.AddNotesActivity">

    <TextView
        android:id="@+id/HeadingNotes"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginTop="16dp"
        android:fontFamily="@font/poppins"
        android:text="Agregar notas"
        android:textColor="@color/orange"
        android:textSize="24dp"
        />

    <ImageView
        android:id="@+id/SaveBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_done"
        android:layout_alignParentEnd="true"
        android:layout_alignTop="@id/HeadingNotes"
        android:layout_marginEnd="6dp"
        />

    <EditText
        android:id="@+id/TittleEditText"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:hint="Titulo de la nota"
        android:fontFamily="@font/poppins"
        android:padding="12dp"
        android:textSize="20sp"
        android:layout_below="@id/HeadingNotes"
        android:background="@drawable/orange_border"
        android:layout_marginTop="16dp"
        android:maxLines="1" />

    <EditText
        android:id="@+id/contentEditText"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:hint="Descripcion"
        android:fontFamily="@font/poppins"
        android:gravity="top"
        android:padding="12dp"
        android:textSize="20dp"
        android:background="@drawable/orange_border"
        android:layout_marginTop="12dp"
        android:layout_below="@id/TittleEditText"


        />




</RelativeLayout>